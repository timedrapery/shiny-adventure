# Five Heaps Cross-Cluster Note

- `upādāna` stays `taking personally` as a headword, but `pañcupādānakkhandhā` is governed as `five clung-to heaps` as a fixed whole expression.
- `saṅkhārā` keeps a heap-context override as `heap of putting-together activities` while preserving its dependent-arising policy elsewhere.
- `viññāṇa` stays `knowing` across both heap and dependent-arising contexts; use the formula records to keep the voice stable.
- `anattā` and `attā` are tied to the heap family through the standard not-self and self-view formulas rather than through abstract metaphysical claims.
